# Pi 远端模型预设同步

设置页面的「官方模型预设库 → 更新预设」直接请求
`https://pi.dev/api/models/providers/{providerId}`，不使用项目自建远端仓库。

## 已实现

- 按本地预设索引同步提供商；无索引时使用六个主流提供商作为起点。
- 六路并发，每个请求超时十秒。
- 解析模型数组、`models` 数组包装、模型 ID 到模型对象的映射。
- 同 ID 的远端模型覆盖本地模型，保留远端未返回的本地模型。
- 保留本地提供商级配置，缺失时从模型推导。
- 请求失败保留对应本地数据；全部失败返回错误，不更新成功时间。
- 更新后清理 Pinia 预设缓存并重新加载索引。
- 元数据兼容生成脚本的 camelCase 格式；前端 IPC 使用 snake_case。

## 当前边界

- 目前为手动更新，没有后台定时更新。
- 未实现 ETag、Last-Modified 新旧比较和自动发现新增提供商。
- 预设继续使用既有 JSON 缓存，不修改用户提供商 SQLite 配置。
- 多个缓存文件尚无跨文件事务，写盘失败可能导致部分更新。
- HTTP 端点已实测返回有效 JSON；仍需桌面 UI 端到端验证。
